# U1 batch 06 — server Plan response disposition

- Independent Astra branch from 8eb839e. No schema migration or provider action. Updated only the Astra API preview after ownership and environment checks.
- Added a shared server disposition for responding, requesting help and submitting completion, with explicit reason codes. Published client-safe items expose this result. Plan UI requires positive per-item response permission in addition to the Plan summary; absent disposition is read-only. Existing response-form validation continues to govern form rendering and completion details.
- Next-client-step selection uses the same eligibility rule. Consultant/system verification steps cannot become a client-response CTA. Pending verification-only work has an explicit focus with consultant or automated-check ownership, including a correct Home owner label.
- Draft eligibility uses the same disposition. Actor, published version, source currentness, optimistic draft checks, submission authorization and evidence validation remain separately revalidated in their owning commands. Malformed structured forms permit help but not completion; no new authority is inferred from the read DTO.
- New authoring already prevents mismatched owner/verification methods. The initial integration fixture correctly failed that validation. The test now deliberately simulates an inconsistent legacy row after valid publication and confirms read-only disposition, no client focus, inactive draft context and rejected draft creation with zero stored drafts. This is defense in depth, not evidence of an authoring bypass.
- API checks: initial five-file run passed all existing checks but rejected the invalid new fixture. Corrected run passed the new legacy test and unit suites, with three existing execution cases timing out at 5 seconds while build/web checks competed. Isolated execution rerun at diagnostic 15-second timeout passed all 15 tests in 10 seconds total; project timeout unchanged. Final availability/focus unit run passed 12 tests. Plan service, Journey and workspace projection suites also passed during this batch. Existing pg deprecation warning remains.
- Web checks: Plan pages/live updates/product response/saved response suites passed 24 tests; final Plan/Journey run passed 14 tests. Full five-package build and root lint passed. Whitespace checked. These overlapping focused runs do not constitute a new full-product regression.
- Browser: isolated synthetic Plan fixture after API readiness. Existing U1 Plan layout harness verifies saved draft opening, keyboard focus/value, blocked collection storage, no page errors and no document overflow at desktop/mobile. No response written. Verification-only and missing-disposition states are unit/integration/component evidence, not dedicated live visual fixtures.
- U1 NOT PASSED. This disposition covers legacy Plan response eligibility, not all final available-action/source-blocker DTOs. Live/Major precedence, canonical action destinations, coordinated Plan views and global CreditPlan migration remain; U4 owns underlying model replacement. No broad U2 or U1 completion report.
