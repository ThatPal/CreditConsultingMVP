# U1 batch 07 — coordinated Plan views and exact-step destinations

- Independent Astra branch, based on ac28008. API read-link and Portal composition changes only; no schema or consequential command changes.
- Final CP-PL-01 requires coordinated views over canonical Plan items. Existing published data supports an Overview roadmap, Actions, and Guidance (including milestones). These are projections of the same item IDs, status, instructions, prerequisites and response/history engine. Guidance and milestones are not counted as Actions. No new task store or assumed sequential dependency was created.
- Overview is now the default read-oriented map. Each row names its type, state, owner and prerequisites and opens the existing execution row. Actions/Guidance selection is URL-backed; query links remain under the existing Plan route and its authorization boundary. Unknown view values fall back to Overview. Missing target steps show an explicit notice, without inventing an item or bypassing client scope.
- Home/Center shared current-focus projection links to the precise item and Actions/Guidance view with encoded query parameters. Plan's current-step CTA uses that same destination convention rather than bypassing the response row for its source link. Related source links remain inside the step. Target rows receive focus/scroll positioning on entry.
- View navigation uses the existing dirty/busy navigation guard. Unsaved input survives choosing Stay; discarding requires an explicit choice. Server-owned counts/currentness/availability, background-update snapshot protection, draft version/actor fencing, history and submission-versus-verification rules remain unchanged.
- Progress visualization remains on Overview; response views omit the extra chart. Three mobile tabs fit one row. Empty Guidance explains the absence of published guidance/milestones rather than creating placeholders.
- Verification: initial three web suites passed 14 tests. New view-separation test passed; the new departure test initially queried the page before its modal exit animation completed, then passed after awaiting the accessible link. Final Plan page suite 9/9 and live-update suite 3/3; API focus/availability suites 22/22. Full five-package build passed after correcting an unsupported Testing Library test option. Root lint passed; final cosmetic changes also received targeted lint and web typecheck. Runs overlap and are not a new full-product regression.
- Browser: isolated synthetic client; non-read API requests blocked after login; collection storage deliberately blocked. At 1440×1000 and 390×844, Overview links reach the exact response view, saved response opens, ArrowDown preserves input focus/value, empty Guidance is honest, no page errors/document overflow. Actual Home link includes the exact item. Eight screenshots under docs/evidence/u1-plan-views; desktop roadmap, mobile roadmap/execution and empty Guidance visually reviewed. Astra API process ownership/environment verified before loading built read-link changes. An immediate readiness probe raced startup; subsequent browser checks and readiness returned successfully. No response edited/submitted.
- U1 NOT PASSED. Decisions and Nurture require final client-safe domain queries and are deliberately not fabricated from response events or legacy NurturePeriod. Global CreditPlan migration and remaining Plan domain semantics belong to U4. Full source blocking/Live/Major focus, reference-state matrix, design tokens and wider screen qualification remain. No broad U2 or U1 completion report.

