# U1 batch 01 — interaction safety and reference query refresh

1. **Branch/SHA:** `codex/astra-production`; U0 accepted in `c54b5d1` after staff checkpoint `38478c5`. This report accompanies the next U1 commit. Other versions untouched.
2. **Treatments:** U0 finished dispositions for 120 routes and 47 shared modules. U1 begins REBUILD of CollectionSurface interaction and shared query plumbing. Existing page composition remains RECOMPOSE/REBUILD; no final screen accepted.
3. **Findings:** F07 PARTIAL advances with typed query factories and one reference refresh boundary. F08 interaction quality improves; F20 gains focused tests/browser evidence. F04 server truth, F10 durable saved views and final visual qualification remain open.
4. **Contracts:** final library keyboard/collection/global-state requirements; GetPortalHome/GetPortalJourney/GetCreditCenter/GetCreditPlan query ownership; frozen realtime invalidation/refetch and Security DTO/authority rules. Events remain refresh hints, never frontend lifecycle mutations or command replay.
5. **Files:** queries/creditWorkspace and tests; LiveUpdates; Home/Journey/Plan/Center/Client360 key consumers; Plan response/verification/builder invalidation; CollectionSurface and tests; reconciliation records and browser captures.
6. **Migrations:** none. No schema, provider or server command changes.
7. **Checks:** 7 inventory/parser tests passed. Focused web run: **11 files / 94 tests passed**, covering refresh, realtime sessions/reconnect, Plan live updates, response/verification/builder recovery, Plan page and collection controls. One worker and diagnostic 15-second case timeout as recorded in U0; project timeout configuration unchanged. Root lint passed. Build result recorded below after completion.
8. **Browser:** isolated Chromium synthetic client at 1440×1000 and 390×844. Collection-only sessionStorage reads/writes deliberately throw. Plan renders, saved response opens, ArrowDown preserves textbox focus/value, no horizontal document overflow or page errors. After login, test harness blocks non-read API requests; no response edited/submitted. Both `docs/evidence/u1-foundation/plan-storage-blocked-*.png` captures visually inspected. They show **old composition during interaction verification, not final U1 design**. Independently scrolling shells produce fixed/sticky full-page capture artifacts; old navigation and sticky overlap remain visual work.
9. **Compatibility:** creditWorkspace retains old cache identities. Sources: legacy Home/Journey, published-credit-profile, PlanVersion/PlanItem queries. Targets: GetPortalHome/GetPortalJourney/GetCreditCenter/GetCreditPlan and scoped CRM variants. Query convergence owns U1; underlying Review/Plan/Cycle adapters retire U3/U4/U6. Tests: creditWorkspace, LiveUpdates, PlanLiveUpdates and existing recovery suites. No shadow state/table.
10. **Remaining:** U1 NOT PASSED. Final tokens/shell/compositions, server focus/counts/currentness/available actions, canonical Action destinations and complete state/visual evidence remain. Title-keyed collection storage is still legacy presentation state; guarding failure does not solve account/resource-scoped persistence. Generic styling/empty copy remain. Plan response/verification/builder now share refresh; other domain mutation handlers must converge in their owning slices. No broad U2 or U1 completion report.

## Changed behavior

Batch completion checks: full `pnpm build` passed web/API/worker/runtime/shared (including TypeScript compilation); root ESLint and whitespace checks passed. The focused 94-test run is not a claim of a new full-product regression run. Browser evidence passed both widths as described above.

- Arrow movement acts only on the region or its eligible directly owned rows. Nested fields, editable text, menus and nested actions retain native keys. Disabled/hidden/inert rows are skipped, modified keys preserved, and the final row is not trapped. Nested collections cannot advance an outer collection.
- Storage failure cannot crash the collection; negative/non-finite offsets are ignored. Cleanup captures the mounted element so scroll position can be saved even when React clears its ref.
- Plan mutations refresh Home/Journey/Center/Plan and cached scoped CRM summaries through one helper. Review/Profile/Plan/Strategy/appointments/Live/Major/services/cycle hints refresh those same projections. Private response-draft hints remain local; unrelated payment and current-user queries are unaffected. Reconnect identity validation, cache clearing and dirty-work guards remain.
