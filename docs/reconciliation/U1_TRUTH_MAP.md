# U1 truth reconciliation

Baseline `44a905b`, source-inspected 2026-09-15. No application changes yet. Frozen target: GetPortalHome / GetCreditCenter / GetCreditPlan / GetPlanAction. Domain DTOs include source versions/currentness, blockers and available actions; commands revalidate authority.

| Fact                        | Current owner / derivation                                                                                         | Conflict or gap                                                                                                                                                   | Target / removal owner                                                                                                               |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Home/current focus          | `api/src/journey/routes.ts` private `projection` → `resolveCurrentFocus` in `journey/projection.ts`                | Tests for blocked/stale Round are useful, but fallback remains ApplicationCycleStage; active NurturePeriod; active Round chosen only through selected cycle       | One server focus projection; U6 removes legacy cycle adapter; U4 removes NurturePeriod dependency                                    |
| Goal selection              | First ACTIVE goal, priority/creation order, one summary                                                            | Final model supports multiple persistent Goals; no one-active-goal uniqueness may be reintroduced                                                                 | Active Goals summary with explicit primary desired-credit presentation                                                               |
| Plan counts                 | Home `summarizePlan(getClientPlan(...).plan)`                                                                      | Original Plan-record count fixed. Still one selected legacy Plan; lacks final all-global-root semantics and shared available-action projection                    | CreditPlan/PlanItem summary, shared across Home/Center/Plan; U4 migration                                                            |
| Plan focus/counts           | `web/src/pages/PlanPages.tsx` computes `currentFocus`, `openActions`, `completed`, `canAct`                        | Duplicates server logic; waiting/UNABLE fallback differs from Home; completed count spans Guidance/Milestones while Home counts Actions                           | Server GetCreditPlan summary and exact typed readiness/action disposition                                                            |
| Plan execution              | `api/src/plans/service.ts`, `execution.ts`, `reconciliation.ts`; Plan/PlanVersion/PlanItem/PlanDependency/PlanPath | Useful publication, submission, draft and concurrency machinery. Frozen target removes separate path lifecycle and consolidates one active global CreditPlan root | Explicit legacy PlanVersion/PlanItem adapter, U4 removal. Do not confuse old standalone PlanAction table with active PlanItem engine |
| Dependencies                | Explicit PlanDependency plus path/visibility rules and execution state                                             | Display order no longer sole dependency source; path persistence still conflicts                                                                                  | PlanItemDependency independent of rank/group; U4                                                                                     |
| Profile currentness on Home | `CreditProfileState` or fallback from latest CreditReview                                                          | Home state and Center published output are fetched independently; no single currentness/basis contract                                                            | CreditProfileState pointer/sufficiency + immutable selected snapshot; U3                                                             |
| Credit Center               | `PublishedCreditCenterPages.tsx` → GET `/api/v1/client/credit-profile`                                             | DTO has `current/history` PublishedCreditReview projections, no composed Plan/current focus/blockers/readiness summary                                            | GetCreditCenter role-safe projection; U3 publication migration                                                                       |
| Review/published history    | `PublishedCreditReview`, CreditSnapshot and Review runtime                                                         | Retain immutable approved content and history. Do not reinterpret stale historical recommendation as current authority                                            | CreditProfileSnapshot + Analysis + ReviewRecommendation, current pointers on ProfileState; U3                                        |
| Appointment                 | Journey query selects latest BOOKED/COMPLETED by descending startsAt, status only                                  | Insufficient for next upcoming appointment, time-sensitive focus and correct timezone/context                                                                     | Appointment-backed server summary; U6                                                                                                |
| Restrictions                | Focus checks Round BLOCKED / Strategy STALE / Plan STALE                                                           | No comprehensive Major coordination/currentness/active Live precedence in the input contract                                                                      | Shared focus selector consumes authoritative restriction and service state, never a new lifecycle table                              |

## Queries, mutations and realtime

Current roots: `portal-home`, `portal-journey`, `published-credit-center`, `consultant-published-credit-center`, `client-plan`, plus Plan response/draft/history roots. Definitions are spread across pages and feature components.

`web/src/LiveUpdates.tsx` already maps publication/Plan domains to the actual collection roots (passes 42–44) and invalidates queries after a repeated connection opens. Keep recipient scoping, session-loss checks, stale-response protections and authoritative reconnect reads. Centralize key factories and mutation invalidation; do not replace those protections with optimistic event-only truth. `PlanResponse.tsx` currently invalidates client-plan/Home/Journey ad hoc; frozen Center's new Plan summary must participate too.

## Required compatibility contracts before U1 code

1. Legacy Journey/Cycle/Nurture → read-only source context. No new persisted focus lifecycle. Remove U4/U6 with explicit tests for no-active-work, stale sources, waiting, blockers and historical records.
2. Legacy published Review/CreditSnapshot → current/historical role-safe Profile/Analysis/Recommendation summary. Remove U3. Fail closed on missing approved fields; preserve source/publication dates.
3. Legacy Plan/Version/Item/Dependency/Path → final Plan read DTO. Remove U4. Preserve private draft recovery and immutable response history; path rows do not become new canonical API concepts.

These are required boundaries, not implemented adapters. No adapter was introduced in U0.

## Tests that must survive recomposition

Dirty/busy Plan refresh protection; private draft identity/version fencing; accepted-write versus failed-read recovery; history/evidence authorization; client submission distinct from consultant verification; source-review pauses; concurrent approval/reconciliation; account/session fencing; no automatic consequential mutation on reconnect or login. Existing passes 01–59 supply regression evidence, not permission to preserve conflicting business contracts.

## U1-01 progress

The shared creditWorkspace query factory now owns reference query identities and refresh boundaries. Plan response/verification/builder and relevant realtime domains refresh Home/Journey/Center/Plan plus cached scoped CRM summaries. See BATCH-U1-01.md. Server-derived focus/counts/currentness and final composed DTOs above are still outstanding; this plumbing does not make the legacy projections canonical.

## U1-02 progress

Shared `api/src/workspace/service.ts` now composes server focus, Action-only Plan counts and publication currentness for Home/Journey/Center/Plan. Plan no longer derives completion percentages or next focus in the browser. Center currentness is independent of immutable publication content, with exact publication basis and source identifiers. Active Round selection no longer depends on the selected legacy cycle. See BATCH-U1-02 and `apps/api/src/workspace/README.md` for adapter removal owners and verification.

The baseline table above is historical; these narrow inconsistencies are repaired. Final global Plan/available-action DTOs, Live/Major precedence, final professional readiness and removal of legacy fallback remain unfinished. No U1 acceptance is implied.

U1-06: per-item client response disposition is shared by published Plan reads, next-step selection and private draft eligibility. Missing per-item permission fails closed in the Portal. Verification-only focus names consultant/system ownership. This is a bounded legacy response adapter, not final global available actions; see BATCH-U1-06.

U1-14: the reference surfaces now share Profile currentness reason copy over the existing workspace DTO. EXPIRED, BASIS_UNCONFIRMED and REASSESSMENT_REQUIRED remain distinct. Browser presentation never recalculates expiry or Plan authority. This closes a presentation mismatch, not the remaining server contract gaps.
