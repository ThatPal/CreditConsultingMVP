# U1 batch 02 — shared focus, Action counts and publication currentness

1. **Branch:** `codex/astra-production`, based on `66c1172`. Independent Astra worktree only; no other version changed.
2. **Treatment:** read-only compatibility composition for Home/Journey, Credit Center and Plan. F04/F05/F07 remain PARTIAL. This is not final screen acceptance.
3. **Contracts:** server-owned counts/focus, distinct Action/Guidance/Milestone semantics, publication versus currentness, source identity, client-safe projections, recovery preservation. Governing sources remain those recorded in reconciliation README and U1 truth map.
4. **Behavior:** Plan summary is computed once by the API. Only Actions count toward progress; cancelled work is excluded; awaiting verification and requests for help remain unfinished. Missing summary disables responses and shows an honest unavailable state. Center and Home consume the same projection. Active rounds are selected directly from client scope instead of only from a selected legacy cycle. Blocked/stale round links identify the actual round. Upcoming/ongoing booked appointments use the earliest applicable start.
5. **Currentness:** a publication is current only with matching CURRENT Profile state, no stale flag and an unexpired assessment. Missing/mismatched basis is conservative. Historical publication content remains unchanged; Center shows a separate expiry/reassessment message. Source publication/Review/Plan/version identities and state timestamp are returned. These reads are not an atomic snapshot across independent requests; commands retain authoritative revalidation.
6. **Files:** `apps/api/src/workspace/{projection,service}.ts`, Journey/Plan/Center adapters; Plan and Center presentation; typed web read shapes; focused regression tests. No schema migration, provider change or new persisted workflow state.
7. **Checks:** API 4 files / 37 tests passed using isolated PostgreSQL 5446 and Redis 6396. Tests cover counts, stale/expired/missing basis, private draft exclusion, cross-projection equality, foreign-client empty projection, unchanged history and existing publication/Plan commands. Web initial recovery run 3 files / 10 tests passed; expanded run 5 files / 30 tests passed; final changed-page rerun 2 files / 10 tests passed (overlapping runs, not additive totals). Full five-package build and root lint passed; final web typecheck and whitespace checks passed. Existing pg concurrent-query deprecation warning remains.
8. **Browser:** real synthetic account in isolated Chromium. Home, Center and Plan at 1440×1000 and 390×844; all six captures in `docs/evidence/u1-workspace/` visually inspected. Real GET responses agree on focus/counts/currentness. No page errors or horizontal document overflow. Harness blocks non-read API requests after login; no response submitted. First run correctly rejected old API output; refreshed only verified Astra API process with built code, waited for readiness and reran successfully. Browser currentness fixture has no publication; expiry/history verification is integration and component evidence, not live expired-profile visual evidence.
9. **Compatibility/removal:** workspace service reads existing published Review/Profile state (U3), PlanVersion/PlanItem and NurturePeriod (U4), and cycle-stage fallback (U6). No expansion of legacy state machines. Currentness preserves publication basis selected by Center. Integration tests enforce one shared summary and private-field exclusion. These adapters retire when the final domain reads replace them; see `apps/api/src/workspace/README.md`.
10. **Remaining:** U1 NOT PASSED. Final tokens, shell/navigation, reference-screen compositions, available-action DTOs and canonical action destinations remain. Live/Major focus precedence and broader state matrix remain. Global CreditPlan migration is U4; professional readiness DTO is U3; legacy stage fallback is U6. Home still displays an empty appointment section; Plan still has the old stacked composition/sticky shell; Center empty-state wording/navigation needs final recomposition. No broad U2 or U1 completion report.
